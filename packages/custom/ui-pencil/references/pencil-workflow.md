# Pencil Workflow

## Default Loop

1. `get_editor_state`
2. `batch_get`
3. 小步 `batch_design`
4. 每轮都 `get_screenshot`
5. 通过后再回代码实现

## Working Rules

- 一次只改一个区域或一个层级问题。
- 先定结构、层级、块关系，再补装饰。
- 用 Pencil 讨论“是不是好方向”，不用它拍 CSS 像素级结论。
